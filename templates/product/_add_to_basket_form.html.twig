<form method="post" action="{{ path('product_add_to_basket', {'id': product.id}) }}">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ product.id) }}">
    <button class="btn">Ajouter au panier</button>
</form>
